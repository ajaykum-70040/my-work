#include <stdio.h>
int main()
{
	int hr, ext, i;
	printf("enter a hour: ");
	scanf("%d", &hr);
	for(i=1;i>40;i++)
	{
		if (hr>40)
		ext  =  (hr - 40)*12; 
		
	}
	printf("%d is amount", ext);
	return 0;
}
